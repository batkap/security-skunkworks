interface:
  display_name: "Security Skunkworks"
  short_description: "Run read-only-first repo security orchestration with reports and requirement extraction"
  default_prompt: "Use $security-skunkworks to audit a local repo in read-only mode, generate findings, threats, requirements, and a fixation plan, and only treat the run as complete when required scanner coverage is full."

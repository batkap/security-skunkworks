# JS/TS Web/API Playbook

- Inspect `package.json`, lockfiles, framework config, Dockerfiles, and CI files.
- Look for tracked secrets, missing `SECURITY.md`, weak auth checks, overly broad CORS, unsafe input handling, and missing additive security scans.
- Prefer additive CI changes over replacing an existing pipeline.


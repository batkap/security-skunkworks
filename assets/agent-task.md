---
role: ${role}
title: ${title}
status: ${status}
owner: ${owner}
started_at: ${started_at}
updated_at: ${updated_at}
completed_at: ${completed_at}
---

# ${title}

- Role: `${role}`
- Repo: `${repo_path}`
- Run: `${run_id}`

## Summary
${summary}

## Dependencies
${dependencies}

## Owned Paths
${owned_paths}

## Commands
${commands}

